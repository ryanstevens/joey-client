

export default (state = {show: false}, action) => {

  switch (action.type) {

    // case 'start_plaid_flow': 
    //   return Object.assign({}, state, {
    //     show: true
    //   });
  
    // case 'plaid_exit': 
    //   return Object.assign({}, state, {
    //     show: false
    //   })
  
    default: 
      return Object.assign({}, state, {
    
      })
  }
};