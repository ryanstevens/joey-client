

export default (state = {show: false}, action) => {

  if (action.type === 'start_plaid_flow') {
    return Object.assign({}, state, {
       show: true
    })
  }
  return Object.assign({}, state, {
    
  })
};