
import { StackNavigator } from 'react-navigation';

const stackConfig = {
  welcome: {
    screen: require('@components/welcome').component
  },
  plaid: {
    screen:  require('@components/plaid').component
  }
};
console.log(stackConfig);
const AppNavigator = StackNavigator(stackConfig);

module.exports = AppNavigator;