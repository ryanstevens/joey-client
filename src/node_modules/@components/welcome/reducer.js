
var foo =0 

export default (state = { message: 'jill'}, action) => {

  if (action.type === 'welcome_add')
  return Object.assign({}, state, {
    message: 'jill '+ (++foo)
  });
  else
    return Object.assign({}, state);
}