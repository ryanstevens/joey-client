
var foo =0 

export default (state = {show: true}, action) => {

  if (action.type === 'start_plaid_flow')
    return Object.assign({}, state, {
      show: false
    });
  
  return state;
}