import { combineReducers } from 'redux';
import { pluckFilter } from '@components';

const reducers = pluckFilter('reducer', 'container');
console.log(reducers);
export default combineReducers(reducers);