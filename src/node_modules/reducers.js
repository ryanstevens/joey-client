import { combineReducers } from 'redux';
import { pluckFilter } from '@components';
import { reducer as formReducer } from 'redux-form'


export default combineReducers(
  Object.assign(
    {
      form: formReducer 
    },
    pluckFilter('reducer', 'root-child'), 
  )
);